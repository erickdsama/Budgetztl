Create a new feature spec for **$ARGUMENTS**.

1. Read any existing specs in `docs/specs/` to understand patterns and avoid overlap.
2. Ask the user clarifying questions about the feature scope, target users, and constraints.
3. Write the spec to `docs/specs/$ARGUMENTS.md` following the template from AGENTS.md.
4. The spec should be detailed enough for the Dev agent to implement without ambiguity.

If $ARGUMENTS is empty, list all existing specs in `docs/specs/` and ask the user which feature they want to spec out.
